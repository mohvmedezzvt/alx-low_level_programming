#include <stdio.h>

/**
 * main - Entry point.
 * @argc: argument count.
 * @argv: argument vector.
 *
 * Return: Always 0 (in Success).
*/
int main(int argc __attribute__((unused)), char *argv[])
{

	printf("%s\n", argv[0]);

	return (0);
}
